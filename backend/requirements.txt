# FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0

# HTTP / Env
httpx>=0.27.0
python-dotenv>=1.0.0

# LangGraph
langgraph>=0.2.0
langchain-core>=0.2.0

# RAG
chromadb>=0.4.0

# Optional: Upstage embedding (or use httpx only)
# (embedding called via httpx in rag.py)
